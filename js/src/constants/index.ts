export const DEFAULT_FLUSH_INTERVAL = 5000;
export const DEFAULT_MAX_QUEUE_SIZE = 50;
export const DEFAULT_MAX_RETRIES = 3;
export const DEFAULT_TIMEOUT = 10000;
export const DEFAULT_ENABLED = true;

export const MIN_FLUSH_INTERVAL = 100;
export const MAX_FLUSH_INTERVAL = 60000;

export const CIRCULAR_REFERENCE_PLACEHOLDER = '[Circular]';
export const MAX_STACK_DEPTH = 10;
